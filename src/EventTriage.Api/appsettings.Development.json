{
  "Logging": {
    "LogLevel": {
      "Default": "Debug",
      "Microsoft.AspNetCore": "Information"
    }
  },
  "Llm": {
    "Endpoint": "https://your-aoai-resource.openai.azure.com/",
    "Deployment": "gpt-4o-mini",
    "DefaultPromptVersion": "v1"
    // For local dev set the API key with user secrets instead of committing it:
    //   dotnet user-secrets set "Llm:ApiKey" "<your-key>"
  }
}
